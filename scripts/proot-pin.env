# Termux proot aarch64 pin — update when bumping the runtime.
# Hyphenated names (proot_5.1.107-81_aarch64.deb) 404; use dotted revisions.
PROOT_DEB_NAME=proot_5.1.107.92_aarch64.deb
PROOT_DEB_SHA256=
